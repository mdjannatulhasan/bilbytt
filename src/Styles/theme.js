import { extendTheme } from '@chakra-ui/react'
const theme = extendTheme({
    components: {
    },
    sizes: {
        container: {
            xl: "1450px",
            '2xl': '1450px'
        }
    }
})

export default theme